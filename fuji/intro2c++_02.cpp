#include <iostream>
#include <math.h>
using namespace std;

int main() {
    string s;
    s = "micky";
    // enter -> endl, '\n'
    // ใช้ << เพื่อแบ่งประเภทข้อมูลในการ output

    cout << s << " : " << s << '\n' << endl << s;
    cout << endl << "/---------------------------------------------------\\" << endl << endl;
    

    
    // x += y; เหมือนกับ x = x + y; เป็นการบวกเข้าตัวเอง

    /*
    Operations

    + = บวก
    - = ลบ
    * = คูณ
    / = หารปัดเศษลง
    pow(เลขฐาน, เลขชี้กำลัง), x*x = ยกกำลัง <math.h>
    squreroot = sqrt(ตัวที่ต้องการหา)

    % = mod คือ หารเอาเศษ (จำนวนเต็มเท่านั้น)
    
    x++ เหมือนกับ x += 1, x = x + 1 เพิ่มค่าใน x หลังจากจบคำสั่ง
    ++x เพิ่มค่าใน x ทันที 
    x--,--x

    */



    

    return 0;
}